import template from './pricebox.html';
import controller from './pricebox.controller';

let priceboxComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default priceboxComponent;
